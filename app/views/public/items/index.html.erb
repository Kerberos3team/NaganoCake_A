<div class="container p-2">
  <div class="row m-3">
    <h2>商品一覧</h2>
    <p class="mt-3 mb-0">(全<%= @items_all_counter.count %>件)</p>
  </div>
  <%= render "items_index", items: @items %>
  <%= paginate @items, theme: 'bootstrap-5' %>
</div>