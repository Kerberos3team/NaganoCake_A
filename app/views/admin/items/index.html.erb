<div class="container px-1 py-2">
  <div class="row">
    <h2>商品一覧</h2>
    <div class="ml-auto p-4">
      <%= link_to "新規商品登録へ", new_admin_item_path, class: "btn btn-success" %>
    </div>
    <%= render "index", items: @items %>
    <%= paginate @items, theme: 'bootstrap-5' %>
  </div>

</div>
